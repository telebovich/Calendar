@page
@model Calendar.Pages.CalendarModel
@{
    ViewData["Title"] = "Calendar";
}

<h2>Calendar</h2>
<table>
    @for (int j = 0; j < 6; j++)
    {
        <tr>
            @for (int i = 0; i < 7; i++)
            {
                <td>
                    @if (Model.CurrentMonth[i, j] > 0)
                    {
                        await Html.RenderPartialAsync("DayPartial", Model.CurrentMonth[i, j]);
                    }
                </td>
            }
        </tr>
    }
</table>
